## Agent skills

### Issue tracker

Issues for this project are tracked in GitHub Issues using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Domain knowledge and architectural decisions are tracked in a single-context layout (`CONTEXT.md` and `docs/adr/`). See `docs/agents/domain.md`.
